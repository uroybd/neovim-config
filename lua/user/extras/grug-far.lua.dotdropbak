local M = {
  "MagicDuck/grug-far.nvim",
}

function M.config()
  local gf = require "grug-far"
  gf.setup {
    -- Options
  }
  local wk = require "which-key"
  wk.register {
    { "<leader>fg", "<cmd>GrugFar<cr>", desc = "Replace" },
  }
end

return M
